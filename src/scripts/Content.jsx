import _ from "lodash";
import React from "react";
import styled from "styled-components";

const Wrapper = styled.div`
  margin-bottom: 1rem;
`;

const Content = () => {
  return <Wrapper>Welcome to the Demo</Wrapper>;
};

export default Content;
